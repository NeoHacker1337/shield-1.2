import { StyleSheet, Platform, Dimensions } from 'react-native';
import { Colors, Fonts, Spacing } from './theme';

const { height: screenHeight } = Dimensions.get('window');

export default StyleSheet.create({
  container: { flex: 1 },
  keyboardContainer: { flex: 1 },

  // --- Matrix Background ---
  matrixBackground: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    flexDirection: 'row',
    opacity: 0.03,
  },
  matrixColumn: { flex: 1, alignItems: 'center' },
  matrixChar: {
    color: Colors.secondary,
    fontSize: 10,
    fontFamily: Fonts.family.primary,
    lineHeight: 14,
  },

  // --- Cyber Grid ---
  cyberGrid: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    opacity: 0.02,
  },
  gridLineHorizontal: {
    position: 'absolute',
    left: 0,
    right: 0,
    height: 1,
    backgroundColor: Colors.tertiary,
  },
  gridLineVertical: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    width: 1,
    backgroundColor: Colors.tertiary,
  },

  // --- Form Layout ---
  scrollContainer: {
    flexGrow: 1,
    padding: 20,
    paddingBottom: 40,
    justifyContent: 'center',
    minHeight: screenHeight - 100,
  },
  innerContainer: {
    backgroundColor: 'rgba(0, 0, 0, 0.85)',
    borderRadius: 25,
    padding: Spacing.lg,
    borderWidth: 1,
    borderColor: Colors.borderLight,
    shadowColor: Colors.primary,
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.5,
    shadowRadius: 20,
    elevation: 15,
  },

  // --- Header Section ---
  topSection: {
    alignItems: 'center',
    marginBottom: Spacing.xl,
  },
  logoContainer: {
    position: 'relative',
    width: 100,
    height: 100,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 20,
  },
  logoGlow: {
    position: 'absolute',
    width: 120,
    height: 120,
    borderRadius: 60,
    backgroundColor: Colors.tertiary,
    shadowColor: Colors.tertiary,
    shadowOffset: { width: 0, height: 0 },
    shadowOpacity: 0.3,
    shadowRadius: 15,
    elevation: 8,
  },
  logoGradient: {
    width: 80,
    height: 80,
    borderRadius: 40,
    alignItems: 'center',
    justifyContent: 'center',
    zIndex: 2,
  },
  logoHexagon: {
    position: 'absolute',
    width: 100,
    height: 100,
    borderWidth: 2,
    borderColor: Colors.secondary,
    transform: [{ rotate: '30deg' }],
    borderRadius: 8,
  },
  logoCircuit: {
    position: 'absolute',
    width: 120,
    height: 120,
    borderWidth: 1,
    borderColor: Colors.primary,
    borderRadius: 60,
    borderStyle: 'dashed',
  },
  title: {
    fontSize: Fonts.size.xxl,
    fontWeight: Fonts.weight.heavy,
    color: Colors.textPrimary,
    textAlign: 'center',
    marginBottom: 8,
    letterSpacing: 2,
    textShadowColor: Colors.secondary,
    textShadowRadius: 8,
  },
  subtitle: {
    fontSize: Fonts.size.sm,
    color: Colors.textSecondary,
    textAlign: 'center',
    lineHeight: 20,
    paddingHorizontal: 10,
    marginBottom: 15,
  },
  statusBar: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 8,
    paddingHorizontal: 16,
    backgroundColor: 'rgba(0,255,136,0.1)',
    borderRadius: 20,
    borderWidth: 1,
    borderColor: Colors.secondary,
  },
  statusIndicator: {
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: Colors.secondary,
    marginRight: 8,
  },
  statusText: {
    color: Colors.secondary,
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
  },

  // --- Inputs ---
  inputSection: { flex: 1 },
  inputWrapper: { position: 'relative', marginBottom: 20, zIndex: 1 },
  inputHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 8,
  },
  inputLabel: {
    color: Colors.tertiary,
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
    letterSpacing: 1,
  },
  eyeButton: { padding: 5 },
  cyberInput: {
    backgroundColor: Colors.backgroundInput,
    color: Colors.secondary,
    padding: 18,
    borderRadius: 16,
    fontSize: Fonts.size.md,
    letterSpacing: 1,
    fontWeight: Fonts.weight.medium,
    minHeight: 56,
    borderWidth: 0,
    textAlignVertical: 'center',
    fontFamily: Platform.OS === 'ios' ? 'Courier' : Fonts.family.primary,
  },
  inputBorder: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    borderWidth: 1,
    borderColor: Colors.tertiary,
    borderRadius: 16,
  },
  inputGlow: {
    position: 'absolute',
    top: -1,
    left: -1,
    right: -1,
    bottom: -1,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: 'transparent',
    shadowColor: Colors.tertiary,
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 3,
  },

  // --- Strength Indicator ---
  strengthContainer: { marginBottom: 20 },
  strengthLabel: {
    color: Colors.textSecondary,
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
    marginBottom: 8,
  },
  strengthBar: {
    height: 6,
    backgroundColor: 'rgba(255,255,255,0.1)',
    borderRadius: 3,
    overflow: 'hidden',
    marginBottom: 8,
  },
  strengthFill: {
    height: '100%',
    borderRadius: 3,
  },
  strengthText: {
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.bold,
    textAlign: 'center',
    letterSpacing: 1,
  },

  // --- Status Messages ---
  verificationStatus: { marginBottom: 20 },
  statusSuccess: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(0,255,136,0.1)',
    padding: 12,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: Colors.secondary,
  },
  statusError: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(255,71,87,0.1)',
    padding: 12,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: Colors.danger,
  },
  statusSuccessText: {
    color: Colors.secondary,
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
    marginLeft: 8,
  },
  statusErrorText: {
    color: Colors.danger,
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
    marginLeft: 8,
  },

  // --- Buttons ---
  cyberButton: {
    borderRadius: 16,
    overflow: 'hidden',
    marginTop: 10,
    elevation: 5,
  },
  buttonGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 18,
    paddingHorizontal: 24,
  },
  buttonIcon: { marginRight: 8 },
  buttonDisabled: { opacity: 0.6 },
  cyberButtonText: {
    fontSize: Fonts.size.md,
    fontWeight: Fonts.weight.bold,
    letterSpacing: 1,
    color: Colors.textPrimary,
  },
  buttonBorder: {
    position: 'absolute',
    top: -2,
    left: -2,
    right: -2,
    bottom: -2,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: Colors.secondary,
  },

  // --- Login Button ---
  loginButtonContainer: { marginTop: 15 },
  loginCyberButton: {
    borderRadius: 16,
    overflow: 'hidden',
    elevation: 5,
  },
  loginButtonText: {
    fontSize: Fonts.size.md,
    fontWeight: Fonts.weight.bold,
    letterSpacing: 1,
    color: Colors.textPrimary,
  },
  loginButtonBorder: {
    position: 'absolute',
    top: -2,
    left: -2,
    right: -2,
    bottom: -2,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: Colors.primary,
  },

  // --- Bottom Section ---
  bottomSection: { marginTop: 20, alignItems: 'center' },
  securityNotice: { marginBottom: 20, width: '100%' },
  noticeBackground: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 15,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: Colors.danger,
  },
  noticeText: {
    color: Colors.danger,
    textAlign: 'center',
    fontSize: Fonts.size.xs,
    fontWeight: Fonts.weight.medium,
    lineHeight: 16,
    marginLeft: 10,
    flex: 1,
  },
  featuresContainer: { alignItems: 'center', width: '100%' },
  featuresTitle: {
    color: Colors.primary,
    fontSize: Fonts.size.sm,
    fontWeight: Fonts.weight.bold,
    letterSpacing: 2,
    marginBottom: 15,
  },
  featuresList: { alignItems: 'flex-start' },
  featureItem: { flexDirection: 'row', alignItems: 'center', marginBottom: 8 },
  featureDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: Colors.tertiary,
    marginRight: 12,
  },
  featureText: {
    color: Colors.textSecondary,
    fontSize: Fonts.size.sm,
    fontWeight: Fonts.weight.medium,
    letterSpacing: 1,
  },
  successContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(0, 255, 136, 0.1)',
    padding: 12,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#00ff88',
    marginBottom: 15,
  },
  successText: {
    color: '#00ff88',
    fontSize: 11,
    fontWeight: '600',
    marginLeft: 8,
    flex: 1,
  },
  warningContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 165, 2, 0.1)',
    padding: 12,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ffa502',
    marginBottom: 15,
  },
  warningText: {
    color: '#ffa502',
    fontSize: 11,
    fontWeight: '600',
    marginLeft: 8,
    flex: 1,
  },
});
